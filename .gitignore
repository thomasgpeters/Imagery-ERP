build/
cmake-build-*/
*.o
*.a
*.so
*.dylib
project_planner
CMakeCache.txt
CMakeFiles/
compile_commands.json
.cache/

# Local config overrides (app_config.yaml itself is tracked with safe defaults)
app_config.local.yaml

# SQLite database files (standalone mode)
data/
*.sqlite
*.sqlite3
